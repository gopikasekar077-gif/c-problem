#include <stdio.h>
int main()
 {
    int num,a=60,b=60,c=60;
    sum=a+b+c;
    if(sum==180){
        printf("valid triangle");
    }
    else{
        printf("not a triangle");
    }
    return 0;
}